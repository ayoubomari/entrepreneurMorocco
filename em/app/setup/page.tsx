import SetupPageComponent from './SetupPageComponent';

export const metadata = {
  title: 'Setup complet : entreprendre et s\'installer au Maroc',
  description: 'Accompagnement clé en main pour créer votre entreprise au Maroc : bilan, fiscalité, installation et premiers clients.',
};

export default function SetupPage() {
  return <SetupPageComponent />;
}